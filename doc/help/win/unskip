SYNTAX: win skip <refnum>
	No longer skip a window while cycling through the window list. This command is used to restore a window that was skipped with the "win skip" command. If no window is specified, the current window is assumed.

PARAMETERS
	<refnum>: (Optional) The reference of the number to skip.

SEE ALSO
	win skip
